create table list
       (lid integer,
       name varchar(30),
       privacy integer,
       primary key(lid))

create table tweet
       (tid integer,
       posttime date,
       body varchar(140),
       
     